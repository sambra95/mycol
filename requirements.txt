# Core scientific stack
numpy
pandas
scipy==1.16.3
matplotlib
seaborn
Pillow

# Machine learning
scikit-learn==1.7.2
scikit-image~=0.25
cellpose==3.1.1.2
# tensorflow==2.16.2              # ⬅ Optional: comment out if not needed
# keras==3.10                      # ⬅ Optional: comment out if not needed
torch==2.5.1
torchvision==0.20.1               # ⬅ Pin to match torch 2.5.1
torchaudio==2.5.1                 # ⬅ (Nice-to-have) match torch version
typing_extensions==4.15.0
--extra-index-url https://download.pytorch.org/whl/cpu   # ⬅ Ensure CPU wheels

# Computer vision
# opencv-python                     # ⬅ REMOVE (conflicts)
# opencv-contrib-python==4.10.0.84  # ⬅ REMOVE (conflicts)
opencv-python-headless==4.11.0.86   # ⬅ Keep only headless
tifffile==2024.9.20

# SAM2 + Hydra
# sam2                              # ⬅ REMOVE (avoid duplicate with git+ below)
git+https://github.com/facebookresearch/sam2.git
huggingface_hub
hydra-core>=1.3.2                   # ⬅ Ensure Hydra is present

# Visualization and web app
plotly==6.3.1
hydralit==1.0.14
hydralit_components==1.0.10
streamlit==1.40.0
streamlit-drawable-canvas==0.9.3
streamlit-image-coordinates==0.1.9
streamlit_image_annotation
